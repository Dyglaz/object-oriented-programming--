#include "classes.h"
#include <iostream>
#include <string>
using namespace std;
forth::forth(string name) : first("n"){
    this-> name = name;
    if(name != "n")
    cout << name << "_" << 5 << endl;
}