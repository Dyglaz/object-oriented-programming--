#include "classes.h"
#include <iostream>
#include <string>
using namespace std;
third::third(string name) : first("n"){
    this-> name = name;
    if(name != "n")
    cout << name << "_" << 3 << endl;
}